server:
  port: 8888
spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/ENG-SERVER/config-files
#          search-locations: file://${user.home}/Desktop/ENG Work File/ENG-SERVER/config-files
