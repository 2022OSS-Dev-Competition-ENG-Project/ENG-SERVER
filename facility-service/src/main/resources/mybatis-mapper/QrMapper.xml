<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.managerservice.mapper.QrMapper">

    <select id="getQRCode" resultType="java.lang.String">
        select facility_qr_location
        from facility
        where facility_name = #{facilityName}
          AND facility_address = #{facilityAddress}
    </select>


</mapper>