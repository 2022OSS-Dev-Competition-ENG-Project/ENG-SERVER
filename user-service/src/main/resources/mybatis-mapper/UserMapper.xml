<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.engproject.mapper.UserMapper">

    <resultMap type="com.example.engproject.dto.UserDto" id="userInfo" >
        <result property="user_uuid" column="userUuid"/>
        <result property="user_email" column="userEmail"/>
        <result property="user_password" column="userPassword"/>
        <result property="user_name" column="userName"/>
        <result property="user_phone_number" column="userPhoneNum"/>
        <result property="user_birth" column="userBirth"/>
        <result property="user_type" column="userType"/>
    </resultMap>

    <update id="SignupUser" parameterType="com.example.engproject.dto.UserDto">
        UPDATE user
        SET user_uuid = #{userUuid}, user_password = #{userPassword}, user_name = #{userName}, user_nickname = #{userNickname},
            user_phone_number = #{userPhoneNum}, user_birth = #{userBirth}
        where user_email = #{userEmail}
    </update>

    <update id="NickNameCheck" parameterType="com.example.engproject.dto.UserDto">
        update user
        set user_access_type = #{AccessType}, user_nickname = #{nickName}
        where user_email = #{userEmail}
    </update>

    <insert id="EmailConform" parameterType="com.example.engproject.dto.UserDto">
        insert into user(user_uuid,
                         user_email)
        values(#{temporaryUuid},
               #{userEmail})
    </insert>

    <select id="registerNickNameCheck" resultMap="userInfo">
        select *
        from user
        where user_nickname = #{userNickName}
    </select>

    <select id="registerEmailCheck" resultType="com.example.engproject.dto.UserDto">
        select *
        from user
        where user_email = #{userEmail}
    </select>

    <select id="findEmail" resultType="com.example.engproject.dto.UserDto">
        select *
        from user
        where user_email = #{userEmail}
    </select>

    <insert id="EmailCode" parameterType="com.example.engproject.dto.UserDto">
        UPDATE user
        SET user_login_key = #{LoginKey}
        where user_email = #{userEmail}
    </insert>
</mapper>