<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.userserver.mapper.UserMapper">
    <insert id="SignupUser" parameterType="com.example.userserver.dto.UserDto">
        INSERT INTO user (email,
                          password,
                          name,
                          nickname,
                          phonenum,
                          birth
        )
        values (
                   #{email},
                   #{password},
                   #{name},
                   #{nickname},
                   #{phonenum}
               )
    </insert>

    <update id="conform">
        update user
        set login_key = 1
        where email = #{email}
    </update>
</mapper>